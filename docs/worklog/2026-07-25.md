# 2026-07-25 — Phase 2 live chat shipped + verified; screen-share gap noted

## Shipped & verified on prod ✅
- **Phase 2 opportunistic live *chat* support** — merged to main, auto-deployed
  (migration v36 ran on prod, DB now at v36). Verified end-to-end in a real
  browser (Claude drove a test user, Derek was admin):
  - Admin `/admin/support` shows stored written requests with an online/offline dot.
  - "Start live session" → **in-app banner** pushed in real time to an online user.
  - User clicks Join → `/support/live` chat page → **bidirectional text chat works**
    (user↔admin messages relayed via Socket.IO room `live_<request_id>`).
  - Offline path exists (time-boxed 30-min emailed link) — **not yet tested live**.
- **Presence fix** (commit after the merge): a user who joined *after* the admin
  kept showing "Waiting for Derek…". Now tracks admin/user presence per session so
  a late joiner is caught up → correctly shows "Derek is here." Re-verified in browser.
- Full suite green: **233 passed**. Deploys all green via GitHub Actions.

## The screen-share question (Derek noticed it's gone)
- The old live support had **WebRTC screen-share** (watch the borrower's screen) + chat.
- It was removed **before this session** — when support.html was rewritten from the
  full-screen live view to the async form (commit `873e0c1`), the **user-side** WebRTC
  client (getDisplayMedia + offer) was deleted. The **server** signaling handlers
  (`webrtc_offer`/`answer`/`ice`) still exist in routes/support.py but are **dormant**
  (nothing triggers them). The admin dashboard still renders the legacy
  "Waiting/Active Sessions" video UI, but it never populates.
- Phase 2 was built **chat-only by choice** (Derek picked "Chat only — screen-share
  can come later"), so screen-share was not rebuilt.

## Not done / possible future (only if motivation returns)
- **Test the offline email-link path** (close user session → Start live → 30-min link
  to the user's email → open it → chat loads). Left untested.
- **Phase 3 — rebuild WebRTC screen-share** on top of the working live chat:
  re-add getDisplayMedia + offer/answer on the user live page, render the stream in
  the admin panel. Meaningful build; WebRTC (ICE/STUN, perms) was the fiddly/buggy part.
- Minor: admin can't set request status (replied/resolved/closed) from the dashboard yet.

## Test artifacts left on prod (clean up if you like)
- Test user: **derekgg+test1@gmail.com** (non-admin).
- One test support request: **support_requests id 1** ("Phase 2 live chat test").

## Project status
Derek's low on motivation here right now — **no live users, project effectively
dormant**. Pausing feature work; this log is for later review, not a to-do list.
